name: kuron_komiktap
description: KomikTap source implementation for Kuron app
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  kuron_core:
    path: ../kuron_core
  dio: ^5.9.0
  html: ^0.15.4
  logger: ^2.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.25.8
  mockito: ^5.4.4
  build_runner: ^2.4.13
