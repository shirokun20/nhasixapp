name: kuron_native_example
description: "AI Translate Demo — ONNX bubble detection, mosaic, multi-provider AI, overlay"
publish_to: 'none'

environment:
  sdk: ^3.10.7
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  kuron_native:
    path: ../

  cupertino_icons: ^1.0.8
  image: ^4.5.4

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  image: ^4.5.4

flutter:
  uses-material-design: true

  assets:
    - assets/sample_manga.webp
